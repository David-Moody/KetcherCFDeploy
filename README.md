# Ketcher Cloudflare Deployment

### Containerised Ketcher served via a Cloudflare tunnel

Create a .env file in the root folder with Cloudflare tunnel token. i.e. TUNNEL_TOKEN=...

  docker compose build
  docker compose up -d
